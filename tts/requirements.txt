# 2026-08-15: XTTS-v2 를 버리면서 torch·TTS·transformers 가 전부 사라졌다.
# 합성은 Gemini TTS HTTP 호출이고, 호출에는 표준 라이브러리(urllib)만 쓴다 —
# google SDK 를 붙이지 않는 이유는 요청 한 종류를 위해 의존성 트리를 늘릴 값어치가 없어서다.
fastapi==0.115.0
uvicorn[standard]==0.32.0
pydantic==2.9.2
