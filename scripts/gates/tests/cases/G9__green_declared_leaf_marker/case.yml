gate: G9
expect: PASS
planted: "배제 문자열을 담은 leaf 에 `# <!-- EXCLUSION-DECL -->` 주석을 달았다 — 배제를 *기록하는* 문장이지 배제한 재료를 렌더하는 문장이 아니다"
green_proves: >-
  이 GREEN 이 없으면 마커 면제가 실제로 동작하는지 알 수 없다. 같은 디렉터리의
  RED(G9__red_excluded_in_leaf)가 마커 없는 leaf 는 여전히 FAIL 임을 지키므로,
  둘을 같이 봐야 "마커가 있을 때만 면제된다"가 증명된다. 그리고
  G9__red_marker_inside_value 가 "마커를 값 안에 박아 자기 면제하는 길"을 막는다.
